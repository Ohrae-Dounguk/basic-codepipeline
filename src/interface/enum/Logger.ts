export enum ELoggerSystemType {
    data = "data",
    bill = "bill",
    cms = "cms",
    epass = "epass",
    sms = "sms",
    bizm = "bizm",
    default = "default",
}

export enum ELoggerType {
    info = "info",
    error = "error",
}
